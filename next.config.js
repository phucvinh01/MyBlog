/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com', 'avatars.githubusercontent.com', 'lh3.googleusercontent.com', 'xutklbjuigzx1sdi.public.blob.vercel-storage.com'],
    },
}

module.exports = nextConfig
