/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', 'lh3.googleusercontent.com', 'xutklbjuigzx1sdi.public.blob.vercel-storage.com'],
    },
}

module.exports = nextConfig
